\usepackage[
    a4paper,
    left=.75in, right=.75in,
    top=.75in, bottom=.75in
]{geometry}

% Required for inserting images and colors in document
\usepackage{graphicx, xcolor}

\usepackage{parskip}  % paragraph spacing

% for creating merged cells, as well as multi-column layout
\usepackage{multirow, multicol}

% captioning subfigures and such
\usepackage{subcaption}
